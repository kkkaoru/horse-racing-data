pre-commit:
  commands:
    shellcheck:
      glob: "*.sh"
      run: shellcheck {staged_files}
    oxfmt:
      glob: "*.{ts,json,jsonc,yaml,yml,md,toml,css,html}"
      run: bunx oxfmt --check {staged_files}
    oxlint:
      glob: "*.ts"
      run: bunx oxlint {staged_files}
    tsc:
      glob: "*.ts"
      run: bun run --filter horse-racing-records tsc
    vitest:
      glob: "*.ts"
      run: bun run --filter horse-racing-records test
    # scripts/ 配下 Python (pywinauto 自動化など) の品質チェック。
    # ty/basedpyright/pytest はプロジェクト全体に対して動くので {staged_files} は渡さない。
    scripts-ty:
      glob: "scripts/**/*.py"
      run: cd scripts && uv run ty check
    scripts-pyright:
      glob: "scripts/**/*.py"
      run: cd scripts && uv run basedpyright --project pyproject.toml
    scripts-pytest:
      glob: "scripts/**/*.py"
      run: cd scripts && uv run pytest
