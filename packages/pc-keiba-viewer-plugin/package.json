{
  "name": "pc-keiba-viewer-plugin",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run format:check && bun run lint && bun run python:check",
    "format": "bunx oxfmt",
    "format:check": "bunx oxfmt --check",
    "lint": "bunx oxlint",
    "python:check": "uv run ruff check src tests && uv run ruff format --check src tests && uv run ty check src tests && uv run basedpyright src tests && uv run pytest",
    "test": "uv run pytest",
    "test:coverage": "uv run pytest"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:"
  }
}
