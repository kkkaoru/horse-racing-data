{
  "name": "horse-racing-records",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash scripts/generate-config.sh && wrangler dev --config wrangler.deploy.toml",
    "deploy": "bash scripts/deploy.sh",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "tsc": "tsc --noEmit",
    "oxlint": "bunx oxlint ."
  },
  "dependencies": {
    "avsc": "5.7.9",
    "hono": "4.7.5"
  },
  "devDependencies": {
    "@types/node": "22.19.10",
    "@vitest/coverage-v8": "3.0.5",
    "vitest": "3.0.5"
  }
}
