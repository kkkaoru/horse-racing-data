{
  "name": "sync-realtime-data-features",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "bun run deploy:worker && bun run queues:configure",
    "deploy:worker": "wrangler deploy",
    "dev": "wrangler dev",
    "d1:migrate": "wrangler d1 migrations apply sync-realtime-data-features-db --remote",
    "d1:migrate:local": "wrangler d1 migrations apply sync-realtime-data-features-db --local",
    "format": "oxfmt --write src test package.json tsconfig.json vitest.config.ts wrangler.jsonc",
    "format:check": "oxfmt --check src test package.json tsconfig.json vitest.config.ts wrangler.jsonc",
    "lint": "oxlint . --tsconfig ./tsconfig.json --type-aware --import-plugin --node-plugin --promise-plugin --deny-warnings --max-warnings=0",
    "queues:configure": "wrangler queues consumer worker remove sync-realtime-data-features-jobs sync-realtime-data-features || true && wrangler queues consumer worker add sync-realtime-data-features-jobs sync-realtime-data-features --batch-size 1 --batch-timeout 5 --message-retries 3 --max-concurrency 4",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "@dsnp/parquetjs": "catalog:",
    "horse-racing-realtime": "workspace:*",
    "hyparquet": "1.26.0",
    "pg": "catalog:",
    "pg-cloudflare": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@types/pg": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "esbuild": "catalog:",
    "miniflare": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
