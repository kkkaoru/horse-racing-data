{
  "name": "finish-position-predict-container",
  "private": true,
  "type": "module",
  "scripts": {
    "artifact:verify": "PYTHONPATH=src uv run python -m predict_lib.artifact_integrity",
    "format": "oxfmt --write package.json pyproject.toml production-artifacts.json DEPLOY.md src/predict_lib/deploy_flags.json src/predict_lib/running_style_cell_routing.json src/predict_lib/stage1_routing.json",
    "format:check": "oxfmt --check package.json pyproject.toml production-artifacts.json DEPLOY.md src/predict_lib/deploy_flags.json src/predict_lib/running_style_cell_routing.json src/predict_lib/stage1_routing.json",
    "lint": "oxlint . --no-error-on-unmatched-pattern",
    "python:check": "bun run python:lint && bun run python:typecheck && bun run python:test",
    "python:lint": "uv run ruff check",
    "python:test": "uv run pytest",
    "python:typecheck": "uv run basedpyright --project pyrightconfig.json"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:"
  }
}
