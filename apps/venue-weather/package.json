{
  "name": "venue-weather",
  "private": true,
  "type": "module",
  "scripts": {
    "backfill:r2-catalog": "uv run --with duckdb python scripts/backfill-r2-catalog.py",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "format": "oxfmt --write src scripts package.json tsconfig.json vitest.config.ts wrangler.jsonc",
    "format:check": "oxfmt --check src scripts package.json tsconfig.json vitest.config.ts wrangler.jsonc",
    "lint": "oxlint . --tsconfig ./tsconfig.json --type-aware --import-plugin --node-plugin --promise-plugin --deny-warnings --max-warnings=0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "tsc": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "miniflare": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
