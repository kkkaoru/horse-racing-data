{
  "name": "venue-weather",
  "private": true,
  "type": "module",
  "scripts": {
    "backfill:r2-catalog": "uv run --with duckdb python scripts/backfill-r2-catalog.py",
    "backfill:r2-catalog:v2": "uv run --with duckdb python scripts/backfill_r2_catalog_v2.py",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "format": "oxfmt --write src scripts package.json tsconfig.json vitest.config.ts wrangler.jsonc",
    "format:check": "oxfmt --check src scripts package.json tsconfig.json vitest.config.ts wrangler.jsonc",
    "lint": "oxlint . --tsconfig ./tsconfig.json --type-aware --import-plugin --node-plugin --promise-plugin --deny-warnings --max-warnings=0",
    "sync:weather-local": "uv run --with duckdb python scripts/sync_r2_sql_to_duckdb.py",
    "test:python": "uv run --with duckdb python -m unittest discover -s scripts -p 'test_*.py'",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "tsc": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "miniflare": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
