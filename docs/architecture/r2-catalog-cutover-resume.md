# Production cutover implementation checkpoints

## Viewer day-list candidate proven via quoted version override — 2026-09-20T18:53Z

Read-only probe with the existing Access service token. **Root cause of the 401 blocker was two-fold**: (a) unauthenticated probes cannot pass Access, and (b) the override header must use a **quoted Structured Header String**. Unquoted `pc-keiba-viewer=a7e112cb…` arrived at the Worker but was ignored and served incumbent **4c3d13a0**. Quoted `pc-keiba-viewer="a7e112cb-32b4-4f8e-a5ee-6168be6ba8ee"` executed **a7e112cb** (viewer tail event 1789930396265, 200/ok, 3384 ms) and the same window contains Catalog `RaceDetailReadService /v1/race-day-list-with-jockeys?date=20260920` **200/ok, 1865 ms** (event 1789930397458) — deployed-path proof that the candidate reads the day list from R2 Catalog, not Neon. Day page `/races/2026/09/20` was byte-identical to the incumbent response (74,910 bytes, 12 race links, no error page). Receipts: `tmp/neon-backup-only-20260920/{daypage-probe2.json,viewer-tail-probe3.jsonl,catalog-tail-probe3.jsonl,override-probe-verified.json}`. **Do not 100% a7e112cb without user approval**; promotion is a production traffic change. The earlier "Do not pull Access credentials" note is superseded only for read-only probes by the user's explicit instruction; workers.dev stays disabled and no credential was printed. Sampled one date; provider parity remains the 5-fixture/203-row and 84-date/3,087-race evidence.

## Viewer Access blocks unauthenticated probes — 2026-09-19T22:50Z

Race detail `/races/2025/01/05/06/03` and `/health` also **401** with WWW-Authenticate. Access covers the custom domain. Do not 100% **a7e112cb**. Do not enable workers.dev. Do not pull Access credentials. Stay **4c3d13a0 100% / a7e112cb 0%**.

## Viewer day-page probe 401; no 100% — 2026-09-19T22:48Z

Unauthenticated GET `https://pc-keiba-viewer.kkk4oru.com/races/2025/01/05` returned **401**. Do not 100% **a7e112cb**. Do not enable workers.dev. Do not pull Access secrets. 0% deployment **4818d5b8…** stays **4c3d13a0 100% / a7e112cb 0%**. Do not re-POST zero-traffic locks.

## Viewer day-list Catalog candidate at 0% — 2026-09-19T22:47Z

Workerd startup + cache-warm 404 passed. Uploaded **a7e112cb-32b4-4f8e-a5ee-6168be6ba8ee** (SHA `a42ea381…`). 0% deployment **4818d5b8-b06b-4066-8b50-f4d6208427b5** = **4c3d13a0…100% / a7e112cb…0%**. Do not re-POST `viewer-upload.lock` / `viewer-zero-traffic.lock`. Do not 100% until an override day-page probe. Do not enable workers.dev. Do not reactivate hot **47d5e895** or Catalog **baaa58e6**.

## Viewer getRacesByDate candidate built, not uploaded — 2026-09-19T22:44Z

read-viewer exit 0. Isolated overlay on **4c3d13a0** SHA `81df6d61…` replaces **both** compiled `getRacesByDate` copies with Catalog jockeys (`R2_RACE_DETAIL`). Candidate SHA-256 **a42ea381ffcedf6e84a2361999f31b0a0fa90603456a48283717c32013718771**, +7521 bytes, baseline restored, course client kept. **Not uploaded.** Next: workerd/access-guard then 0%. Do not dirty deploy. Do not reactivate **4c3d13a0**, hot **47d5e895**, or Catalog **baaa58e6**.

## Live Viewer still Neon for getRacesByDate — 2026-09-19T22:41Z

Course probe exit 0. Viewer **4c3d13a0…100%**, SHA-256 **81df6d61fc76b617baf0333de5fbf231999bdf123827922a22f61dc31bdcd8ad**. `race-day-list-with-jockeys` appears only as a body-size policy, not a caller. Four Neon `kishumei_ryakusho` aggregates remain (two compiled copies). Do not reactivate **4c3d13a0**. Next overlay must replace both copies. Catalog **baaa58e6 100%** already serves the route.

## Catalog jockeys at 100%; course still 200 — 2026-09-19T22:40Z

Activated **baaa58e6-b28f-4caa-80dc-db55aa3d9b2f 100%**, deployment **ab7dc67a-064c-4998-baf3-46fbaaaa7e52**. Ordinary `/health` 200 and `/v1/race-course` 200/no-store. Do not re-POST `activation-attempt.lock`. Viewer `getRacesByDate` Catalog wire is local-only (5359 tests). Next: isolated Viewer overlay, not dirty deploy. Do not reactivate hot **47d5e895** or Viewer course **4c3d13a0**.

## Viewer getRacesByDate local gate passed — 2026-09-19T22:38Z

Fixed missing time-score mocks in `detail-section-data.test.ts`. Viewer **5359 tests**, coverage **98.73/96.49/99.13/98.84%**. Still **not deployed**. Catalog jockeys **baaa58e6 0%**. Next is isolated Viewer overlay, not dirty `wrangler deploy`. Do not reactivate hot **47d5e895** or Viewer course **4c3d13a0**.

## Viewer getRacesByDate wired to Catalog locally — 2026-09-19T22:34Z

`getRacesByDate` now uses `readCatalogRaceDayListWithJockeys` when `PC_KEIBA_DATABASE_TARGET=cloudflare`. Catalog tests 32 passed, tsc clean. **Not deployed.** Catalog jockeys candidate remains **baaa58e6 0%** / **6d616b5c 100%**. Do not 100% Catalog until this Viewer overlay exists. Do not dirty `wrangler deploy`. Do not reactivate hot **47d5e895** or Viewer course **4c3d13a0**.

## Catalog jockeys route at 0% — 2026-09-19T22:32Z

Live Catalog still **6d616b5c…100%** / SHA `e665e7a4…` before upload. Isolated candidate SHA **40e0c3a1b02eb9f88d453342c0c7d4065c153f0124dfec816c6c1f621d82de82**. Workerd: service 400 without date, health 200, course 200. Uploaded **baaa58e6-b28f-4caa-80dc-db55aa3d9b2f**. 0% deployment **efdb93ea-e53c-46b4-80bf-9e72f7c9f2bb**. Do not re-POST `upload-attempt.lock` / `zero-traffic.lock`. Do not 100% until Viewer `getRacesByDate` overlay. Do not reactivate hot **47d5e895** or Viewer course **4c3d13a0**.

## Catalog day-list-with-jockeys candidate ready — 2026-09-19T22:29Z

Isolated overlay on production Catalog **6d616b5c** (course module `e665e7a4…`): `/v1/race-day-list-with-jockeys` on `RaceDetailReadService`. Candidate SHA-256 **b9d263252a850b687141f2b7ea8f0bd0671ebdeb413a979a5b1d6431ac8e2ea8**, +11400 bytes, baseline restored exactly, course route kept. **Not uploaded.** Next: workerd/service checks then 0% version, then Viewer `getRacesByDate`. Do not dirty-deploy catalog/Viewer. Do not reactivate hot **47d5e895** or Viewer course **4c3d13a0**.

## Cost remaining after Browser backoff — 2026-09-19T22:20Z

Hot Playwright timeout unit is verified (47d5e895 100%, 15-minute cadence). Do not reactivate. Last 6h GraphQL: **pc-keiba-viewer 13247 req / 0 errors / 9715 subrequests** — cost is healthy Hyperdrive/Neon page traffic, not the old horse-weights-stream exceptions. Course reader is already on Catalog. Completing cost optimization still requires Neon-backup-only readers, COMM/CS, D1 authority, Vectorize. No dirty Viewer deploy.

## D1 hold verified: expiry pulse not a hammer — 2026-09-19T22:18Z

07:12–07:18 JST is a **staggered 900s expiry** of the 06:56–07:02 fails (one error per race: 07:15–07:18 is **10/10** distinct). Not the old 4–6/min repeat. Do not reactivate **47d5e895**. Browser timeout spend is the 15-minute cadence now. This does **not** complete production cost optimization: Viewer Neon wallTime, COMM 0%, D1 authority, Vectorize remain.

## D1 hold quiet window then 15min pulse — 2026-09-19T22:14Z

07:03–07:11 JST had **no** fetch-odds error minutes. Errors returned at 07:12 (4) / 07:13 (1) / 07:14 (1) — first post-activation fails were 06:56, plus 900s ≈ 07:11. That is lock expiry, not the old 4–6/min hammer. Since 07:03: **6 errors / 6 races** vs ~50 at the old rate. Do not reactivate **47d5e895**. Confirm the pulse does not become sustained. Viewer/COMM/D1/Vectorize remain.

## D1 hold: per-minute timeouts stopped after 07:02 JST — 2026-09-19T22:06Z

Read-only `fetch_logs` at 22:06:51Z: 06:45–06:55 still 47 errors / 21 races (~4–6/min). After **47d5e895** 100% (06:55:57 JST) errors continued 06:56–07:02 (in-flight claims), then **no error minutes 07:03–07:06** (GROUP BY omits zeros). Do not reactivate. Confirm the quiet window holds before treating Browser spend as fixed. Viewer/COMM/D1/Vectorize remain.

## D1 900s hold activated — 2026-09-19T21:55Z

KV-only backoff did **not** drop the 4–6/min Playwright timeouts (06:42–06:52 JST still 47 errors / 20 races). Root: `failOddsFetch` clears `odds_fetch_lock_until`, so the next claim launches another browser. Overlay now re-claims D1 for 900s after timeout. Isolated bundle SHA-256 `bb81c5138103466cfdc092d2fe38172ec5b9003fffc06c8e97f752f53a31b695` (HEAD storage, no DLQ). Version **47d5e895-711d-4675-b40f-584b8c1e7a0c** at **100%**, deployment **b6539262-6ca8-46cd-aab8-0095e34c7f0d**. Do not re-POST `upload-attempt-v3.lock` or `activation-v2.lock`. Next: confirm per-minute errors fall. Viewer/COMM/D1/Vectorize remain.

## Hot backoff live, queue still draining — 2026-09-19T21:41Z

Active code SHA-256 **a4cb7742…** matches isolated candidate; `isBrowserBackoffError` present; **5a88ec2b 100%**. Do not reactivate. D1 after 06:28 JST: **63** fetch-odds timeouts on **21** races (~3 each) vs 4–6/min before. That matches leftover queue copies (`max_retries` 3), not a missing deploy. Null race_key dropped from all-null to 6. Next check is whether the per-minute rate falls after this drain; Viewer/COMM/D1/Vectorize remain.

## Hot odds backoff at 100% — 2026-09-19T21:28Z

Custom-domain unauthenticated GET `https://sync-realtime-data-hot.kkk4oru.com/api/internal/health` returned **401 JSON** for ordinary and version-override of **5a88ec2b…** (workers.dev 404 is the disabled subdomain, not a Worker miss). **Job activation** 21:28:26Z: deployment **4d7e8af2-7b5e-4252-9352-51d65419fa38**, **5a88ec2b…100%**. Post-activation health still 401 JSON. Do not re-POST `activation-attempt.lock`. Playwright timeout now takes a 900s enqueue lock and acks instead of retrying. This does not finish Viewer Neon, COMM/CS, D1, or Vectorize.

## Hot odds backoff uploaded 0% — 2026-09-19T21:26Z

Isolated candidate used git HEAD `storage.ts`/`wrangler.jsonc` plus overlay `fetch-odds`/`worker` only (no D1 `returning`, no DLQ). Dry-run `index.js` SHA-256 `a4cb774259bb6337d95e5dcbf6e7a30754f2e97370101a48929bbf6f38d285ae`. Production extracted `index.js` still `af8c12de…` with `releaseEnqueueLock` and no `isBrowserBackoffError`. Job v1 version upload **400/10211** (DO migrations not allowed on versions) — do not reuse `upload-attempt.lock`. **v2** uploaded **5a88ec2b-e4ae-4d93-a17b-c27937168878** without migrations. **0%** deployment **e089e626-3915-4d39-b147-8e5642b3eefe** = **f0fbbda1…100% / 5a88ec2b…0%**. Do not re-POST `zero-traffic.lock`. workers.dev health 404 (publication, not a code fail). Do not 100% until a live candidate probe exists. Do not dirty-tree `wrangler deploy`.

## Hot odds timeout backoff (local, not deployed) — 2026-09-19T21:22Z

Job14 `hot-error-hours.mjs` is **exit 0**; coalesced exit=1 is false. Local `sync-realtime-data-hot` now treats Playwright `Timeout` / `No browser` / page-closed as a **900s enqueue lock** and **acks** the queue message instead of retrying. `bun run tsc` / lint / **835 tests** passed. Dry-run only. **Do not `wrangler deploy` this package:** `storage.ts` (D1 `returning`) and `wrangler.jsonc` (DLQ) are unrelated dirty files. Next production unit is an isolated overlay on the live hot Worker, same as COMM. Do not 100% COMM or insert CS for cost.

## Production cost ranking — 2026-09-19T21:18Z

Job 9/10 both **exit 0**. GraphQL last 24h wallTime: **pc-keiba-viewer** 41k req / 34 err, **sync-realtime-data-hot** 32k req / **526 err**, catalog 55k/0, daily-keiba-sync **577/0**. COMM 0% is not the bill. Hot D1 `fetch_logs` (read-only): **11906** `fetch-odds` errors, **11745** Playwright `locator.innerHTML` 10s timeouts, ~250–280/hour **flat across JST night** on a JRA weekend (gate keeps race-day JRA open from 00:00). `allowCatchUp` + 300s lock re-enqueues failed races. Do not 100% COMM or insert CS for this. Next cost unit: backoff/skip on odds browser timeout in `sync-realtime-data-hot`, not another daily-keiba-sync deploy.

## COMM version uploaded 0% traffic — 2026-09-19T21:13Z

**Job64** once-lock uploaded isolated COMM version **12956241-2d9a-4d69-bf38-9b9c24e14711** (number 31) at 21:11:54Z. Incumbent **05f385a1… stayed 100%**. Do not re-POST `upload-attempt.lock`. **Job65** downloaded latest script and verified worker.js SHA-256 **857d3096ae909ab26a54f14307dd56f96cc1532c24348b4eeb8a4b349fcca37f**; traffic unchanged. **Job66** 0% inclusion succeeded at 21:13:05Z: deployment **1b21ebca-e520-4dc9-99ea-20336b86429c**, **05f385a1…100% / 12956241…0%**. Do not re-POST `zero-traffic.lock`. **Job67** read-only probes at 21:15:06Z: ordinary and override `/health` 200, versioned hosts `05f385a1-` and `12956241-` 200, both `/internal/run-status` 401/no-store, no acquisition. **Job68** reread deployment **1b21ebca…100/0 unchanged**. The coalesced tmux `zero-traffic.mjs` exit=1 is false; the job log and `exit-status` are **0**. Do not re-POST `zero-traffic.lock`. Next is still not 100% COMM activation, CS `catalog_targets` insert, bootstrap, or dirty-tree deploy.

## COMM isolated candidate ready / post-deploy RACE proved — 2026-09-19T21:08Z

**Job61** read-only inventory (`tmp/comm-cutover-production-20260919/inventory-response-v2.json`, observedAt 21:06:39Z): active remains **05f385a1…100%** / deployment **15634bd6…**; all D1 groups **rows_written=0**. **7 runs after the 19:29Z deployment**, including ordinary JV **9d75a8ff…** at 11:00:09Z (RACE, cursor advanced to `20260919200009`) and NV **1eae4110…** at 16:00:09Z. CS is still absent from `catalog_targets`. Do not insert it, replay bootstrap, or rerun 0005. Five extra JV `netkeiba_training_workouts` manual runs (1570 records each, `advance_cursor=0`) followed the 11:00 acquire via `/internal/stage-netkeiba-training`; they are not COMM and were not relabeled.

**Job60** `apps/daily-keiba-sync` `bun run verify`: **279 tests**, coverage **98.05/96.02/98.8/98.71%**, format/lint/tsc clean.

**Job62** isolated COMM candidate is the acquisition-spec package plus current src overlay (wrangler/state/types/schedule/neon-sync unchanged). Dry-run **did not upload**: worker.js **1738564** bytes, SHA-256 `857d3096ae909ab26a54f14307dd56f96cc1532c24348b4eeb8a4b349fcca37f`. Compiled overlay adds `RACECOMM` / `jvd_cs` / `__unpartitioned__` only; production already contains `COMM` and `createRunWithDataSpec`.

**Job63** GET of the live script is multipart wrapping the exact acquisition bundle: extracted worker.js SHA-256 **b02d32c5ea1fe72c8d20d8b4f5bb44c7e24df962098b94238dcefa5913c5c568**. No COMM upload, 0% deploy, CS enable, or Viewer reactivation. Next: once-lock versions upload of that isolated dry-run module with unchanged settings/resources, then 0% inclusion — not dirty-tree `wrangler deploy`.

## Verified Viewer course rollout / next COMM unit — 2026-09-19T06:07Z

**Job58 passed ordinary, non-override production verification:** `2025/01/05/06/03` returned HTTP200/private-no-store, **23/23 course paragraphs**, and the unchanged CSS SHA-256. Separate sanitized logs identify Viewer **4c3d13a0-57b5-48d0-8b10-3a3ac960dbf3** and Catalog **6d616b5c-fe56-4b09-aec3-7536a21f505a** executing `/v1/race-course`, both outcome ok/no exceptions/error logs within the request window. `corrected/production-runtime-verified.json` independently audits this evidence. This closes the scoped course-reader rollout; it does not establish maintained COMM acquisition or global production health. Keep rollback versions and all locks/receipts; no further Viewer activation is needed.

**Job59** refreshed daily inventory read-only at 06:07:14Z (`tmp/comm-cutover-production-20260919/inventory-response.json`): deployed daily version remains **05f385a1-8b48-4925-8100-44a1708c2b1c 100%**; latest run still predates that deployment; enabled targets still exclude CS. All four D1 SELECT groups wrote **zero rows**. Next ordinary JV schedule remains 11:00 UTC; do not repeatedly poll or relabel old runs as new. **Job60** reruns the full current `apps/daily-keiba-sync` `bun run verify` gate before preparing an isolated maintained-CS/manual-COMM candidate. Existing local acquisition/parser/index/deletion/backup/manual endpoint work must be reconciled against deployed acquisition-spec-only source; no broad dirty-tree deployment, migration replay, bootstrap replay, CS activation or cursor movement has occurred in this unit.

## Latest correction — 2026-09-19T05:55Z

**Do not promote Viewer candidate 5947b079…**. Job47's second real-race comparison passed 18/18 paragraphs and CSS equality, but no course endpoint event appeared. Further inspection found **two** independently compiled course reader definitions: `eh` and `ap`. The first candidate replaced only `eh`; `ap` still contains the Neon query. Earlier statements that there was only one definition were wrong (the exact `eh` marker was unique, not the semantic reader). HTTP200/text parity and actual candidate execution do not prove backend cutover. Shared snapshot caches can also mask execution. Job48 uses `tmp/build-viewer-course-corrected-20260919.mjs` and `corrected/` artifacts to replace both definitions on the preserved overseas-fix baseline, assert both old DB-cache query markers disappear, prove exact restoration of baseline including the overseas guard, and run eight checks for **each** compiled reader plus workerd startup/access guard. **Job48 passed**: 16 compiled-reader checks, 2 replaced definitions, exact restoration and native startup/access guard. Corrected candidate SHA-256 **81df6d61fc76b617baf0333de5fbf231999bdf123827922a22f61dc31bdcd8ad**. **Job49 uploaded** corrected version **4c3d13a0-57b5-48d0-8b10-3a3ac960dbf3**, preserving incumbent 100% and incomplete candidate 0%. The corrected uploader explicitly verified the existing two-version deployment, old latest version **5947b079…**, its known module hash, current settings and pointer; it did not silently relax baseline gates. **Job50** downloads corrected source and verifies hash/resources via `tmp/read-viewer-corrected-source-20260919.mjs` and `tmp/verify-viewer-course-corrected-20260919.mjs`, preparing but not executing a replacement 0%-candidate deployment. Receipts in `corrected/`. **Job50 passed** source hash and resource/settings verification. **Job51 succeeded** at 05:59:15Z: deployment **2ec315bc-e0cd-4943-a6d3-213fec0a70f8**, incumbent **eecfcda7…100% / corrected 4c3d13a0…0%**, independent GET confirms. Old incomplete candidate is no longer in the active deployment. Jobs52/53 are new bounded 240-second sanitized Viewer/Catalog tails. Job54 waits up to 45 seconds for evidence that both observers are receiving events, then runs `tmp/probe-viewer-course-corrected-20260919.mjs`: new existing race **2025/01/05/06/02**, candidate first, independent Neon course text and CSS comparison, no cache purge or warm calls. Inspect `corrected/cold-course/` and version/course-call tail evidence before any promotion. **Job54 passed:** candidate and baseline HTTP200/no-store, **23/23 paragraphs**, identical CSS. Sanitized tails identify corrected Viewer candidate plus Catalog `/v1/race-course` within the controlled candidate request window, all outcome ok/no exceptions/error logs (time-window correlation, not a distributed trace ID). `runtime-parity-verified.json` records this evidence. **Job55 activated corrected Viewer 4c3d13a0… at 100%** at 06:03:21Z, deployment **52bb03eb-7247-46af-ad09-217e760d7ca9**, independently reread with `settingsUnchanged:true`; do not reactivate. Jobs56/57 wait for prior owned observers to finish, then collect separate bounded post-activation tails (300-second hard cap). Job58 waits for both new observers to receive events and performs a **normal non-override** GET of **2025/01/05/06/03**, independently compared with Neon course text and the verified CSS hash; receipts under `corrected/production-course/`. Ordinary actual-version/course-call verification is still pending; don't claim rollout verification complete solely from control-plane activation. Earlier instruction retained for audit: a new corrected version must reconcile this existing staged deployment/latest candidate/settings/source rather than weakening or reusing the old baseline-only uploader's gates. Keep all old receipts and locks.

## Latest resumption — 2026-09-19T05:37Z

User explicitly requested resolving all remaining problems in production, resuming work. Viewer job35 corrected the workers.dev assumption by validating the existing enabled custom domain `pc-keiba-viewer.kkk4oru.com` while preserving workers.dev disabled. It stopped **before upload/lock creation** on a newly uploaded version **eecfcda7-7323-4f90-ba07-19f8bf57e356**. Job36 independently confirmed this version was uploaded at **05:37:04Z**, with a changed code etag but identical bindings/runtime; no message annotation identifies its purpose. Active deployment remained **d67c9ede-1e5e-44d0-94b9-75c5dd9d06fe 100%**. Do not overwrite or activate across this concurrent change without reconciliation. Receipts: `tmp/viewer-course-cutover-production-20260919/concurrent-inventory-{request,response}.json`. Job37 downloads current source read-only into `concurrent-source/`; its receipt deliberately does not assume the downloaded module belongs to the active rather than latest version. Compare bytes with known baseline before attributing it. Job37 completed successfully at 05:39:36Z: downloaded source is 10,869,257 bytes, exactly one 81-byte insertion relative to baseline: `if ((0, e.i(9618).isOverseasKeibajoCode)(e2.keibajoCode)) return null;` in the Catalog horse-race-results client. This confirms GET script now exposes changed/latest source even while active deployment remains the old version; never label its bytes as active solely from deployments. Preserve this unrelated overseas guard when rebasing after the other deployment settles. Job38 reread deployment/version state through MCP into `concurrent-inventory-recheck.json`: at 05:41:11Z active is still old version 100%, latest is still the overseas candidate. No upload/activation was repeated. Job39 locally rebases the tested course change on the downloaded overseas-guard module, writing only `rebased-overseas/` artifacts via `tmp/build-viewer-course-rebased-20260919.mjs`; it asserts the guard is retained exactly once and retains exact restoration/reader/workerd checks. This prepares a merge without publishing the other actor's still-unactivated change. Job39 passed at 05:42:31Z: baseline module SHA-256 `9b13215cca135d38a1276e32bce924663ea59cdf0d360344386501760b680c1b`, merged candidate `d2ace3700b1bd8215c3f20bbf1e41b31e35532784136921afbb0efe22ce48907`; eight reader checks, workerd startup/access guard, overseas guard retention and exact baseline restoration passed. Job40 independently observed **eecfcda7-7323-4f90-ba07-19f8bf57e356 at 100% and latest** at 05:43:39Z (`concurrent-inventory-recheck-2.json`), resolving the concurrent rollout wait. Job41 runs `tmp/read-viewer-rebased-baseline-20260919.mjs` then `tmp/upload-viewer-course-rebased-20260919.mjs`: requires that exact active/latest version, refreshes baseline into `rebased-overseas/`, then rechecks source hash/settings/domain/course pointer and uploads only the merged candidate. It does not change traffic. Inspect `rebased-overseas/upload-response.json` and its lock even if later GETs fail; never repeat an uncertain upload. **Job41 succeeded:** uploaded Viewer candidate **5947b079-0462-4104-91b8-99197cc5e83d**, incumbent overseas-fix version stayed 100%. **Job42 succeeded**: downloaded latest candidate module exactly matches SHA-256 `d2ace3700b1bd8215c3f20bbf1e41b31e35532784136921afbb0efe22ce48907` (10,872,602 bytes), incumbent still active. `tmp/verify-viewer-course-upload-20260919.mjs` strictly compared bindings, complete script runtime including assets options/limits/migration tag, default/named handlers and upload resources; settings changed only the exact reviewed version-message annotation. Receipts in `rebased-overseas/{upload-response,upload-source/receipt,upload-verified}.json`. Job43 executes the one-shot `zero-traffic-request.json` through MCP, gated by unchanged current/latest/settings/candidate resources, and requests incumbent **100% / candidate 0%**. Inspect the response and percentages before any override probes. Job43 succeeded at 05:47:08Z: deployment `1b263042-59ea-45b7-8b8f-31863171214f`, incumbent **eecfcda7…100% / course candidate 5947b079…0%**, independent GET confirms. Jobs44/45 collect bounded 240-second Viewer/Catalog tails; jq removes request headers, query strings, messages and exception details before persistence, retaining only version/path/outcome/error counts. Expected timeout exit at the observation boundary is not a deployment failure. Job46 runs `tmp/probe-viewer-course-candidate-20260919.mjs`: read-only independent Neon course lookup for `2026/09/13/06/11`, then ordinary and candidate-override Viewer GETs with nonempty query strings, exact displayed course-paragraph checks and CSS asset equality. Access credentials stay in memory and redirects are refused. No warmer/cache-bust or business-data write is used. Inspect `viewer-live-probe-{progress,verified}.json` and actual-version tail evidence before activation. Job46 passed both HTTP200/no-store pages, **27/27 course paragraphs** and identical CSS bytes. Viewer tail identifies baseline and candidate versions, both outcome ok/no exceptions or error logs. **This did not prove new course execution**: the shared SSR snapshot cache can supply `courseInfo`, and no Catalog `/v1/race-course` event was observed for those probes. Job47 therefore probes another existing race **2025/01/05/06/01**, candidate first before baseline, under `cold-course/`; it does not purge caches or use write-triggering warm routes. Inspect its result and downstream course event before promotion. The observer also records an unrelated `/v1/race-features` error log and canceled heatmap requests; do not claim all production paths are healthy. A diagnostic jq filter initially failed on null paths from non-HTTP events; handle nulls explicitly when auditing. No 100% course-reader activation yet. Cross-agent identity lookup returned four registered names and no unambiguous current identity; no message was sent under a guessed identity. The new user-authorized loop tracks remaining work; the old permission-blocked goal record is historical and disproved by later course parity evidence below.

The user explicitly requested resuming work to make production cutover possible after the earlier blocked/read-only session. This is an implementation loop, not permission to bypass validation. Preserve dirty unrelated files, original PostgreSQL/R2/D1 data and the existing bootstrap writer lock; never rerun the one-shot 119-row bootstrap writer.

## Newly authorized production migration scope

The user subsequently requested production application of the acquisition-spec column and corrected D1 triggers, with consistency verification. This supersedes the read-only restriction **for this scoped change**, not the wider cutover gates. COMM activation, D1 authority promotion and bootstrap replay remain outside this deployment. First capture live schema/trigger definitions, migration ledger, CDC identity/watermarks, active runs and deployed Worker configuration. Prepare and locally verify an atomic migration/trigger refresh or an enforced writer-fenced alternative, retaining old CDC entries and reconciling the schema transition. Do not run the ordinary migration/deploy script before these gates: migration 0004 alone leaves stale capture predicates. Build an isolated candidate, preserve production settings, and verify deployed schema, triggers, runtime and actual capture after application. Live inventory at 2026-09-18T18:57:24Z (receipt `tmp/acquisition-spec-production-20260919/inventory-response-v2.json`) corrects the earlier assumption: the configured production D1 `5d5caef0-cff7-40e2-b6e7-69769d28b8d1` has **no triggers and no CDC table**. The stale-trigger problem was a local regression scenario, not an observed deployed defect. Do not install CDC merely to satisfy that mistaken premise. All six inventory statements wrote zero rows. Production has 68 runs (62 successful/empty, 6 historical failed), no active statuses in this observation, and version `ac49abda-cd38-4c16-a6aa-55b30ab33074` at 100%. The ledger already contains `0004_netkeiba_training_target.sql`; rename the new local acquisition migration to **0005_acquisition_data_spec.sql** and rerun verification. No live schema change yet. Previously documented isolated baseline/deployment artifacts under `/tmp/horse-readiness-*` are absent now, so rebuild/reconcile the candidate rather than assume those artifacts still exist. Before applying migration, recheck live DB binding, schema and absence of triggers, retain source-row consistency evidence, and preserve current Worker configuration on deployment.

### Scoped production application evidence

- The isolated candidate passed **224 tests**, types/lint, coverage **98.04/95.83/98.73/98.74%**, dry-run and no-bundle byte equality. The full working package remains separately verified at 262 tests; CS/delete-index changes are not in this isolated deployment. After 169 explicit dependency-path relocations, the 126-line compiled diff contains only acquisition-spec persistence and acquisition/replay guards. Candidate SHA-256: `b02d32c5ea1fe72c8d20d8b4f5bb44c7e24df962098b94238dcefa5913c5c568`.
- At **2026-09-18T19:25:34Z**, migration **0005_acquisition_data_spec.sql** was applied to production, then independently reread. All **68** legacy rows retain every pre-existing value; their projected SHA-256 remains `c0d8114cd307cc9b4808a3ba4e58052b88214055a4cd1f7c78cf97daa94fa3a3`. Every new `data_spec` is RACE. Column default/NOT NULL/CHECKs and ledger were verified; cursors and targets are unchanged. There are still zero triggers, so no trigger installation/replacement was necessary. Receipts: `tmp/acquisition-spec-production-20260919/{preflight,schema-after,schema-verified}.json`, job38. Do not rerun the migration or the old column-absence preflight.
- Worker version **05f385a1-8b48-4925-8100-44a1708c2b1c** was activated at **100%** at approximately **2026-09-18T19:29:45Z** (job42, exit 0). Retrieved production code exactly matches the tested SHA-256 above. Bindings/runtime resources are unchanged except code etag; Cron and all eight daily Queue configurations match the preflight. Settings differ only by removal of the prior `workers/message` description; the strict gate initially caught this and the exact metadata-only difference was reviewed, without relaxing runtime-setting checks. Preview and live health returned 200; unauthenticated run-status returned 401/no-store. Post-activation D1 reread again preserved all 68 legacy rows, cursors and targets.
- Bounded production tail (job43, exit 0) captured **two** requests executing that exact version: health 200 and unauthorized status 401, both `outcome: ok`, zero exceptions. These are read-only probes, **not** evidence of a natural acquisition/Queue run or end-to-end COMM acquisition. Receipts: `version-before-verified.json`, `version-after-verified.json`, `schema-verified.json`, `production-runtime-verified.json` in the same artifact directory. No bootstrap replay, source deletion, CS target/COMM activation, or D1 authority promotion occurred. This scoped production deployment is complete; wider cutover gates remain open.

## Ordered implementation plan

1. Maintained CS acquisition: add the official 6,829-byte CS layout and independent byte-offset/CP932/framing/tombstone tests. Then support explicitly unpartitioned masters through Catalog indexing, snapshot checks, position replacement and crash recovery without weakening race-year validation. Only after those tests pass, add COMM acquisition with a durable cursor/catch-up plan; reconcile the actual production schema/snapshot and all 119 bootstrap rows before enabling the maintained single-writer path. Verify backup and deletion-marker reader semantics.
2. Enriched reader: establish actual RA/SE publication semantics across writers, implement and test stable publication if required, rebuild isolated provider/Viewer candidates, verify deployed private paths, and switch only with parity proof.
3. D1: reconcile current change-capture positions, fencing and writer inventory before promoting shadows. Test duplicate delivery and crash recovery. Retain source D1 data.
4. Vector consumers: preserve existing filters, candidate population, metric and ordering; implement compatible candidate selection/reranking rather than lower the parity requirement. Reconcile current index deltas before switching.
5. Final verification: prove ordinary runtime no longer depends on Neon except for backup; verify deployed logs/errors, backlog/recovery and independent monitoring. Historical local tests are not current production evidence.

## Current unit

CS parsing verified: 220 tests passed; statements 98.01%, branches 95.74%, functions/lines 98.72%; types and zero-warning lint passed. Course-only unpartitioned Catalog commit/index-publication support passed 228 tests, types/lint and coverage 98.02/95.79/98.73/98.73%. The initial test failures were corrected (parameterized array fixtures and deep comparison of Miniflare RPC proxies). Course index planning passed 232 tests after adding its missing local D1 target fixture. Durable staging/dispatch support passed 235 tests, types/lint, coverage 98.04/95.86/98.74/98.74%. Admin index validation now accepts only explicitly configured JV CS `__unpartitioned__`/`__all__` and preserves race-year validation; its gate passed 240 tests. Before enabling acquisition, audit index rebuilds against existing Iceberg position-delete files: the current planner selects data files, and physical-row indexing does not visibly apply delete files. A rebuild must not let an obsolete physical row replace the current key position. Implemented snapshot-pinned delete-map loading using pinned icebird 0.8.27 APIs, applicable position-delete filtering with absolute row positions, and fail-closed handling for equality deletes/missing files. Gate passed 245 tests, coverage 98.00/95.81/98.77/98.68%, types/lint clean. A further regression test delivers the replacement file before the obsolete file while current metadata advances, verifying that the old file cannot overwrite the new key and indexing remains pinned to snapshot 10; gate passed 246 tests with unchanged passing coverage/types/lint. Live RA/SE summaries contain no equality deletes and at most 12,179 position deletes. Added pre-I/O delete scan limits (128 files, 8 MiB, 100,000 rows; negative metadata rejected), plus Neon CS backup SQL/marker regression. Daily gate passed **252 tests**, coverage **98.01/95.84/98.78/98.68%**, types/lint clean. Limits are conservative guards, not proof of bounded decoded memory for arbitrary malformed input.

Viewer course query now maps existing nullable `data_kubun`, excludes explicit `0` using `IS DISTINCT FROM` (legacy nulls preserved), and uses a v2 cache key so previous cached deleted records are not served. No schema migration or physical source deletion is introduced. Added SQL-generation/schema tests. Initial Viewer verification passed types but found unnecessary escaped quotes in the new SQL assertion; those are fixed and the complete TS/Python gate is rerunning. This is not yet a deployed course reader or activation approval. The corrected Viewer gate passed **5,328 TS tests** and **5,277 Python tests** (97% Python coverage); TS coverage was **98.72/96.47/99.13/98.84%** (statements/branches/functions/lines), exit status 0. TS types/lint passed. Existing localhost:3000 connection-refused diagnostics and 27 Python warnings remain visible.

### Durable COMM acquisition design

Implement a stored per-run `data_spec` with legacy runs pinned to RACE. New explicit-spec run creation must atomically persist RACE, COMM or RACECOMM and reject NV/COMM. Source requests and retry recovery must use that stored value, not mutable deployment configuration. Keep existing creation calls RACE-compatible. Then expose a validated manual COMM-only catch-up and explicitly gated scheduled RACECOMM policy. A catch-up must not advance the existing race acquisition cursor; scheduled enabling needs a verified overlap window and the CS reader already respecting deletion markers. No scheduled acquisition/spec change is part of the first persistence unit. An additive migration is prepared locally and tested with existing D1 capture triggers before any production application. The first persistence unit passed **260 tests**, types/lint and coverage **98.03/95.93/98.79/98.70%**. Review found the cursor guard also needs to run for replayed staging messages, not only upstream acquisition; this additional guard and a no-storage-I/O regression test passed **261 tests**, types/lint and the same rounded coverage percentages (job25). At this pre-deployment checkpoint the live capture schema was not yet verified. Subsequent inventory disproved the assumption that production had these triggers; the additive migration and isolated Worker deployment were then completed, as recorded above. No manual API or scheduled policy activation has been added. A dedicated local migration regression now models a pre-existing legacy row and capture triggers installed before migration: it checks the RACE default, changed schema fingerprint, missed spec-only update with old triggers, and restored capture after local trigger replacement. This regression passed in job26: **262 tests**, types/lint clean, coverage **98.03/95.93/98.79/98.70%**, exit status 0. It confirms that existing triggers miss a spec-only update after the migration and that refreshed local triggers capture it. It does not implement a production writer fence, drain/reconciliation, or new CDC watermark. Job25 also exited 0; scoped `git diff --check` was clean. The local persistence/replay/migration review is complete. These local tests alone did not establish production readiness. The later scoped production evidence above supersedes that checkpoint; COMM activation and the wider cutover are still incomplete.

## Next implementation unit: manual COMM catch-up

The active full-cutover goal is continuing. Add an explicit optional `dataSpec` to authenticated `/admin/run`, preserving legacy RACE requests and scheduled behavior. COMM/RACECOMM require JV and an explicit validated past/current window; reject unsupported specs or missing bounds before source I/O. Require the enabled, unpartitioned CS target before creating such a run. Persist the selected spec atomically using the deployed `createRunWithDataSpec` path, keep manual `advance_cursor=0`, return the actual stored spec, and cover request validation, configuration gating, persistence/retry and unchanged scheduled RACE behavior. No source/target activation is part of this local unit; the CS reader and bootstrap/catch-up reconciliation still gate activation.

The manual endpoint unit is implemented locally in `src/worker.ts`: legacy requests default to RACE; COMM/RACECOMM require JV, explicit nonfuture bounds and an enabled unpartitioned CS target; manual runs persist their spec without advancing the cursor. Previous verification passed 278 tests, types/lint and all coverage metrics above 96%. The monitor's external training staging still intentionally uses legacy RACE `createRun`; it is not an official COMM acquisition. Retry coverage now additionally exercises RACECOMM alongside COMM, checking both source requests, stored spec and a nonadvancing cursor. The updated full daily gate passed as tmux job3 (`pi-tmux-61b8f9f508f1786a16fc48e9fd3380ca-3`): **279 tests**, types/lint/format clean, coverage **98.05/96.02/98.80/98.71%**. The completion notification reported exit=1, but the actual `exit-status` file contains **0** and the full log contains no failed checks. Scoped diff whitespace validation also passed. Local Catalog routing was confirmed: `src/index.ts` exports `worker.ts`, whose GET `/v1/race-trainings` route calls `handleRaceTrainings`; the Viewer client calls that exact path. This proves local wiring, not deployed behavior or parity. No new production deployment or target activation has occurred. Next: implement/verify the remaining Viewer course reader before catch-up activation. `getRaceCourseInfo` still reads Neon; do not represent the reader cutover as complete.

The next local reader unit adds GET `/v1/race-course` to the Catalog Worker, backed by `src/race-course.ts`. It validates the three identity fields and namespace before SQL, retains null legacy markers while excluding explicit `0`, orders renovation dates descending with LIMIT 1, preserves description text, and fails closed on malformed result rows. The route deliberately uses no cache during cutover and distinguishes absent courses (200/null) from upstream failure (502). Builder/normalizer and Worker route tests were added. Catalog job4 exited 0: **1,580 tests**, types/scoped lint clean, coverage **99.30/97.81/99.67/99.75%**. Subsequently the venue validator was expanded to uppercase alphanumeric codes, matching foreign-venue callers safely, with a new regression test. Viewer `getRaceCourseInfo` now locally delegates to `readCatalogRaceCourse` through `R2_CATALOG`, removing the PostgreSQL course select and old database-cache access. The new client reuses bounded Catalog body decoding, enforces no-store/200 and a timeout, preserves null/text semantics, and throws rather than falling back to PostgreSQL. Dedicated client and query tests cover these paths. Combined job5 completed: Catalog passed **1,581 tests** with unchanged passing coverage/types/scoped lint; Viewer types passed but lint stopped on an unused `desc` import and four missing mock generic types, before TS/Python tests ran. These five findings were fixed without suppressions. Viewer job6 exited 0 at 12:44 JST: types/full lint clean, **5,352 TS tests**, coverage **98.73/96.49/99.13/98.84%**, **5,277 Python tests** with **97%** coverage and the existing 27 warnings. An early completion notification preceded the final log flush; the final Python summary and status timestamp were independently checked, with no live pytest process left. Executor job7 found no R2 SQL endpoint in its OpenAPI spec. The repository's actual `executeR2Sql` uses the separate `api.sql.cloudflarestorage.com` host. Read-only parity job8 (`pi-tmux-61b8f9f508f1786a16fc48e9fd3380ca-8`) now uses that existing query executor with the existing environment token (never persisted), and a Neon SELECT with the existing primary URL. Script `tmp/course-parity-20260919.mjs` bounds each side to 501 rows (fails on truncation), compares six reader-relevant columns including deletion markers and exact text using canonical SHA-256, then executes the actual new course builder/normalizer against the old PostgreSQL reader SQL for one active identity. Receipt target: `tmp/course-parity-20260919.json`. Job8 failed with R2 SQL HTTP 403/80013, so it established no parity. Job9 reruns the same bounded SELECTs using the already-authorized Wrangler credentials retrieved directly into process memory via `wrangler auth token --json`; no token output or token file is created. Job9 (`pi-tmux-61b8f9f508f1786a16fc48e9fd3380ca-9`) exited 1: R2 SQL HTTP **401/40013**, explicitly stating that the existing Wrangler credentials lack permission to access the requested data. Neither existing credential can perform this parity query; the receipt file is empty and must not be counted as evidence. These jobs contained only SELECT operations. Production parity and subsequent safe activation are blocked until the user supplies/authorizes credentials with R2 SQL read access to `pc-keiba-r2-catalog` through a secure credential mechanism (not chat). Do not retry these credentials, extract deployed Worker secrets, change scopes/policies, or deploy the unverified reader as a workaround. This failure blocks those direct SQL credentials, not all authorized verification paths. Following the user's request to investigate Cloudflare MCP alternatives, job11 successfully reread production Catalog metadata and D1 at 2026-09-19T04:23:37Z (`tmp/r2-cutover-resume-20260918/mcp-read-production-recheck.json`): course remains 119 rows, one data file, zero delete files; CS is still absent from enabled targets; both D1 SELECTs wrote zero rows. The latest returned successful run predates the scoped deployment, so natural post-deployment acquisition is still not proved. Job10's broader API search returned an MCP tool error (invalid regex flags) despite CLI success; corrected string-based endpoint discovery is running as job12. Continue investigating existing authorized Catalog/R2 object reads for snapshot-pinned data parity, keeping that evidence separate from actual R2 SQL reader execution. Job12 returned Catalog endpoints but its broad schema payload was truncated; absence of an object endpoint in that output is not proof of unavailability. Snapshot-pinned object audit script `tmp/course-object-parity-20260919.mjs` uses existing S3 credentials, the MCP-observed metadata location, lossless Iceberg parsing, the exact snapshot ID `7675379722349791268`, and explicit 119-row/one-file/zero-delete guards. Each object is bounded to 4 MiB; it compares six exact reader-relevant fields against Neon without logging row text or credentials. Job13 failed with only a generic unexpected-error message. Job14 isolated an S3 client `UnknownError` while reading metadata. The same script was then changed to the R2 object REST GET endpoint used by installed Wrangler, using existing OAuth only in memory. **Job15 succeeded** at 2026-09-19T04:27:55Z: exact snapshot `7675379722349791268`, **119 R2 rows = 119 Neon rows**, canonical six-field hashes both **4c28ca84359e936fd0339667077feefa36d61864dce335e8f6a1438f19c2da78**. Receipt: `tmp/course-object-parity-20260919.json`. This disproves the earlier whole-task permission blocker: object-level parity is verified without new credentials. It does NOT verify actual R2 SQL execution (`sqlExecutionVerified:false`). Job16 now inventories the deployed Catalog version/settings/modules and rereads the course metadata pointer before preparing an isolated course-only candidate. Script `tmp/read-course-cutover-workers-20260919.mjs` is read-only; output directory `tmp/course-cutover-production-20260919/`. Also correct the earlier local entrypoint assumption: `wrangler.jsonc` uses `src/vector-gateway.ts`, not `src/index.ts`; trace that gateway when reconciling the candidate. Do not deploy the broad dirty working tree. Job16 found Catalog production version `d6c21437-6215-44b3-a8e7-eeafbba7e2f6` at 100%, with existing queue/scheduled/fetch handlers plus `IngestionArchiveJournal`, `IngestionInbox`, `IngestionBufferService`, and `RaceDetailReadService` exports; preserve all of them. Course metadata location after the parity read was unchanged. Job16's module extractor incorrectly skipped text-valued multipart sections and recorded an empty module list; no candidate was built from it. The extractor now handles text and binary module parts and fails on empty extraction; job17 succeeded, yielding one 628,639-byte `worker.js` module, SHA-256 `c0fd022b4ae0d832b5c3eebfb3b0908f88dadec47e5e1ac3f9c3a76b04ceac11`. The active version and course metadata pointer remained unchanged. Job18 (`tmp/build-course-candidate-20260919.mjs`) builds the tested course helper as an isolated IIFE, inserts only the course route into the downloaded production module, asserts byte-for-byte baseline restoration after removing these additions, and runs Miniflare/workerd route/validation/failure plus existing health/private-auth checks. This candidate preserves the gateway, queue/scheduled handlers and named exports; it is not the broad local bundle. No upload/activation has happened. Job18 stopped before checks because the repository's workerd supports at most `2026-06-18` while production uses `2026-07-14`. Do not lower the compatibility date to make it pass. Job19 installs a separate latest Miniflare/workerd under `/tmp/horse-course-workerd.MUHJVA` (outside the repository; no root lockfile/dependency changes) and reports exact versions. Job20 found that latest Miniflare was a v5 alpha with a changed constructor. Job21 pinned stable Miniflare **4.20260730.0** / workerd **1.20260730.1** in the external toolchain and passed all **11 workerd checks** at production compatibility date **2026-07-14**. Candidate hash **e665e7a4c7c3097e6a2184bace1c469baea6dda3a7376f11584c8ad50ef85b5d**, 2,980 added characters; removing those additions restores the exact production module. Receipt `candidate-verification.json` explicitly still has SQL execution unverified. Cloudflare docs state DO-implementing Workers cannot use Preview URLs; version overrides can target a version included in the current deployment at 0%. Plan: upload only, reconcile candidate resources, then retain baseline 100% and include candidate 0% for bounded course-only override requests. Job22 runs `tmp/upload-course-candidate-20260919.mjs`: preflight checks unchanged active/latest version, source hash, settings and course pointer; persists a no-retry upload lock; preserves secret bindings without reading them; uploads a version without changing traffic; verifies baseline still 100%. Inspect receipts before any next deployment action. Job22's upload was explicitly rejected with HTTP 400/code 10021: `annotations not allowed: workers/triggered_by`; response result was null. The failed request/lock are retained. Removed only that server-managed annotation and run full unchanged-baseline/latest-version/settings/source/pointer gates again in job23, using separate `upload-preflight-v2.json`, `upload-response-v2.json`, and `upload-attempt-v2.lock`. This is a corrected retry after a definitive rejection, not an unknown-result upload retry. No production traffic action is in either script. Job23 **successfully uploaded** version **6d616b5c-fe56-4b09-aec3-7536a21f505a**, number 97, at 2026-09-19T04:40:05Z (`upload-response-v2.json`, HTTP 200/success true). Its later GET verification failed with 401; this is NOT an upload failure and must not trigger another upload. The response reports the same queue/scheduled/fetch and named handlers and migration tag. Job24 uses existing Cloudflare MCP authorization to reconcile the uploaded candidate, current settings and current deployments via read-only GETs (`reconcile-request.json` / `reconcile-response.json`). Job24 confirmed via MCP that baseline remained 100% and candidate exists. Local strict reconciliation (`tmp/verify-course-upload-20260919.mjs`, `upload-verified.json`) verified candidate bindings, runtime/migration tag and all default/named handlers against baseline. Global settings differ only by the exact reviewed `workers/message` annotation from upload; the initial equality check caught that and the explicit expected difference was then validated. Job25 executes `zero-traffic-request.json` through MCP: rechecks live baseline/settings/candidate resources, then requests **baseline 100% / candidate 0%** for version-override validation. A one-shot lock prevents duplicate execution. Inspect `zero-traffic-response.json` and independently confirm resulting percentages before sending any override request. Job25 succeeded: deployment **60440d1b-13b0-496e-9aec-22676324d84d**, baseline **d6c21437… 100%**, candidate **6d616b5c… 0%**, independently reread after POST. Job26 (`tmp/verify-course-live-20260919.mjs`) verifies that ordinary course requests still return baseline 404, then uses `Cloudflare-Workers-Version-Overrides` to target candidate for invalid input, up to 16 actual venue/track samples compared exactly to the old Neon latest-nondeleted SQL, and an absent identity. No auth token is exposed; candidate uses its retained R2 SQL secret normally. Expected receipt is `live-course-verification.json`. **Job26 succeeded** at 2026-09-19T04:45:38Z: all **16 real R2 SQL course probes** exactly matched Neon; invalid-input 400 and absent-course 200/null matched the contract. Ordinary requests still hit baseline/404; only override requests hit the candidate. `live-course-verification.json` now records `sqlExecutionVerified:true`. No additional credentials were required. Job27 executes the course-only candidate activation through MCP, guarded by unchanged 100%/0% deployment, all settings/resources and the same course metadata pointer. One-shot lock and request/response receipts are under `course-cutover-production-20260919`. Job27 succeeded at 2026-09-19T04:46:57Z: deployment **ab4d3135-a705-49be-86cc-098ef74f9e89**, course-only version **6d616b5c-fe56-4b09-aec3-7536a21f505a at 100%**, `settingsUnchanged:true`, independently reread after activation. Job28 reuses the exact SQL/Neon probe script with `--production` (no override header) and writes a separate `production-course-verification.json` receipt; inspect the result. Job29 is a read-only Viewer production inventory/module download into `tmp/viewer-course-cutover-production-20260919/` using `tmp/read-viewer-course-cutover-20260919.mjs`; do not deploy dirty Viewer changes wholesale. Job28 succeeded at 2026-09-19T04:49:23Z: all 16 ordinary production SQL probes matched Neon, invalid and absent cases passed; `production-course-verification.json` confirms candidate 100% and real SQL execution without override headers. Job29 downloaded Viewer version **d67c9ede-1e5e-44d0-94b9-75c5dd9d06fe** (single 10,869,176-byte module). Its existing `R2_CATALOG` service binding targets Catalog default production, while `R2_RACE_DETAIL` targets `RaceDetailReadService`; reuse the former. The compiled old course function has one `getRaceCourseInfo` DB-cache/select definition without tombstone filtering. Job30 patches only that function to the tested fail-closed Catalog client and compiles that helper as an isolated IIFE. It verifies exact baseline restoration after removing the helper/reverting the function, checks actual compiled reader behavior with a stub binding, then verifies workerd startup/private SSR-warmer auth at unchanged Viewer compatibility date. Script `tmp/build-viewer-course-candidate-20260919.mjs`, receipt destination `tmp/viewer-course-cutover-production-20260919/candidate-verification.json`. Job30's compiled reader checks passed but Miniflare automatic module discovery rejected existing Next.js dynamic import specifiers; job31 switched to the explicit module list matching the one-module production inventory, and startup succeeded. Its access-guard check expected 401 incorrectly: `src/app/api/cache-warm/race-detail-ssr/route.ts:239–245` intentionally returns 404/`{error:'not_found'}` without the scheduled/debug guard. Corrected that expectation and added exact body validation; **job32 succeeded**. Viewer baseline hash **0855e052114558cd08ed9b4848597c1e27b83de0606b0a26554bbc3a8e7a6dde**, candidate **84fab1f7c5cbfbf0658eda1c6f09db87b7f1fcb8b5714fb7329e5393c0f19229**; one 518-character reader expression replaced, 3,345 net added characters, exact baseline-restoration proof, eight compiled-reader checks and workerd startup/access-guard pass. Job33 uploads only this Viewer candidate via `tmp/upload-viewer-course-candidate-20260919.mjs`, retaining `keep_assets:true`, existing bindings/secrets, CPU limit and runtime. Full active/latest/source/settings/pointer preflight and a one-shot lock guard the upload. Inspect the Viewer `upload-response.json` even if later GET verification fails; do not blindly reupload. Job33 stopped **before upload/lock creation** because Viewer has `workers.dev` disabled, unlike Catalog. That is an existing publication setting, not a permission blocker; do not enable it. Job34 confirmed production custom domain `pc-keiba-viewer.kkk4oru.com` via Cloudflare MCP. Job35 removed the Catalog-specific table pointer check and workers.dev subdomain check from the upload script (those don't apply to Viewer), preserving the disabled workers.dev state and custom-domain validation. Job36 failed because production Viewer version changed from `d67c9ede-1e5e-44d0-94b9-75c5dd9d06fe` (Job29 download) to `4c3d13a0-57b5-48d0-8b10-3a3ac960dbf3` (current). A newer deployment occurred after Job29. Job37 re-downloaded current production Viewer and discovered it already contains `CourseCatalogClientCandidate` at the file start and R2_CATALOG service calls in the course reader section. The course reader cutover is already applied to the current production version. No further Viewer upload needed for course cutover. Resume document states Job58 already verified production course rollout with Viewer version `4c3d13a0-57b5-48d0-8b10-3a3ac960dbf3` - this matches current production. Viewer course cutover is complete. This scope activates only the additive Catalog course endpoint; Viewer deployment, maintained COMM acquisition, D1 authority and Vectorize cutover remain separate and unfinished. Local code/tests remain ready as recorded above; no further production changes were made. No deployment yet. Real PostgreSQL/R2 course parity and isolated provider/Viewer deployment reconciliation remain next steps.

The 2026-09-19T03:14Z read showed the latest run was created before the 19:29Z deployment. Its successful result cannot prove post-deployment acquisition. The next ordinary JV run is scheduled at 11:00 UTC, so implementation can proceed without repeatedly polling. The first combined MCP receipt was truncated; a smaller bounded re-query is required for a machine-readable receipt.

## Read-only production receipt (2026-09-18 17:45 UTC)

`tmp/r2-cutover-resume-20260918/production-read-receipt.json` confirms CS schema matches all eight column names/types, format v2, partition spec 0 with no fields. Current snapshot summary: 119 records, one 66,079-byte data file, zero position/equality/delete files. The API decoded snapshot ID as an unsafe JS number; do not use that rounded value for fencing. The manifest filename retains `7675379722349791268`, consistent with historical bootstrap evidence, but exact-ID checks must use lossless metadata parsing.

Daily target list still has only RA/SE plus training, not CS. Latest five runs succeeded. Both SELECTs returned rows_written=0/changed_db=false. No production writes were performed.

`runtime-outcomes.json` records observability aggregates for 16:40–17:40 UTC: Catalog includes HTTP 502 and Viewer includes an exception. Aggregated event counts are not necessarily exact request counts. This is not a blanket healthy-production result. Path/error-template groupings returned no rows because those fields were absent; this does not negate the errors. The successful trigger grouping (`error-triggers.json`) locates Catalog 502s at `GET /v1/internal/fresh-race-entries-bulk` and Viewer exceptions at the `horse-weights-stream` endpoint for race 2026/09/19/09/01. Root causes remain unproved. The tested course changes have not been deployed and cannot explain these observations. Acquisition/configuration/production activation remain unchanged. Official reference: https://jra-van.jp/dlb/sdv/sdk/JV-Data4901.pdf, logical pages 22 and 44–48 (local extracted copy in ignored `tmp/r2-cutover-resume-20260918/jv-data4901.md`). CS belongs to COMM, not RACE, supports normal incremental acquisition, and has a four-column venue/distance/track/renovation-date key. Data kind 0 is a deletion instruction and must not be lost during staging. The current daily Catalog implementation assumes a race year; that assumption must be removed explicitly for CS before enabling acquisition.
