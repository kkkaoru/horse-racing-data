# Ban-ei 83/10 wrote v8 (2026-08-16)

Observed 05:11 JST by `pi-optimize-developer`. Read-only. Not a tonight fix.

## Fact

`race_finish_position_model_predictions` 0816 `83/10` is
`banei-cb-v8-window2011-wf-15y` (10 rows, `prediction_generated_at`
2026-08-15 19:47:22Z). Same-day `83/01`, `83/03`, `83/06` are
`banei-cb-v9-sim-2011`. No 0815/0816 race has two model_versions at once.

## Why (deterministic)

`apps/finish-position-predict-container/src/predict_lib/cell_routing.json`
ban-ei:

- default variant `sim` → `banei-cb-v9-sim-2011`
- rule `grade_code in {"E"}` → variant `base` → `banei-cb-v8-window2011-wf-15y`

`cell_router.py` documents the same split.

Replica R1 `BEGIN READ ONLY` on `nvd_ra` and
`race_entry_corner_features` for 2026-0816 venue 83:

| race  | grade_code    |
| ----- | ------------- |
| 01–08 | `' '` (space) |
| 09    | `E`           |
| 10    | `E`           |
| 11    | `Q`           |
| 12    | `' '`         |

83/10 matches the E rule. 01/03/06 do not.

08-15 local log `2005-ban-ei-83-11.log` already shows
`[cell-routing] race=...83:11 ... -> base` and
`models: ["banei-cb-v8-window2011-wf-15y"]`. Same mechanism.

## Not known

Container stdout for tonight's 83/10 (debug off). Not required: grade +
rule close the explanation.

83/09 is also `E` and not yet generated tonight. The same rule would
select v8 if it is scored later.
