# Margin feature restoration: frozen next diagnostic

- Old NAR common-schema table has six completely missing columns across all12,331 teachers: speed_index_avg_5, speed_index_best_5, field_strength_avg_speed, field_strength_top3_speed, last_race_margin_to_winner, speed_index_avg_5_diff_from_race_avg. Thus the first comparison had zero comparable stored margins; it did not establish a scale error.
- JRA unit check: eight2020–2023 event fields /103 rows. After matching the canonical **SE inner join RA**, current `/10`, `0000 -> NULL` rules reproduce both stored mean and best on103/103. Unjoined raw SE incorrectly disagreed on three horses. Preserve both audit generations; RA presence is part of the contract.
- Await the NVD history RA metadata snapshot before reconstruction. Retain a ledger of orphan prior SE rows; do not delete raw records or omit current active teachers.
- Reconstruct only from strictly prior same-source histories, using canonical10-year lookback and most recent5 known finishing records. Apply the already verified2018MyTaizan finish1 overlay explicitly to derived history membership; do not invent its missing margin.
- Raw histories begin20000101. For targets before20100101, fewer than5 available prior records cannot prove a complete window: withhold mean/best. Preserve a known latest margin independently. Withhold whole-field aggregates and ranks if any runner has such an incomplete window. Keep every active horse.
- Recompute six missing columns **and their two dependent input ranks** (speed_index_avg_5_rank_in_race, speed_index_best_5_rank_in_race). Leaving the old tied-all-missing ranks would be inconsistent. Nullable SQL RANK semantics (not dense ranks) apply only to certified complete windows; these are features, not predicted finishing ranks.
- `margin_features.py` implements aggregation/censoring;163 tests,8 modules100% coverage, configured Ruff/format/basedpyright pass in logs/jra-margin-contract-001.log. Verify its complete-window field aggregates against canonical race_context_cte before staging.
- New staged parquet must preserve all12,331 identities and all other columns/labels exactly. Keep source input hashes, missingness counts, censored groups, raw-orphan ledger and source/query lineage. No overwrite of the117-column baseline.
- Only after these checks: maximum8 fixed500-iteration Yeti fits, same JVD+NVD117 inputs/cohorts/weights. Compare against priority-common-source-001/jvd-plus-nvd. Do not add the static3 features or modify objective/parameters. No later data may rescue failed development selection.
- This restores six usable feature families and updates two dependents, not the51 absent168-schema columns, the299 full legacy rows, foreign rival completeness, actual incumbent replay or full PIT certification. No promotion yet.
